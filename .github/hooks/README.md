# Hooks

This folder defines repository-level hook commands for Copilot customization.

- `pre-tool-use.json`: runs fast validation before tool execution
- `post-tool-use.json`: runs sanity checks after tool execution

Adjust commands to include stack-specific checks for your project.
