# Project Agents

This repository defines custom Copilot agents under `.github/agents/`.

## Available agents

- `sdlc-orchestrator`: plans and executes workflow, release, and setup tasks
- `docs-guardian`: validates and improves `README.md` and `CHANGELOG.md`

## Usage intent

Use agents for task-focused execution with consistent quality checks and explicit outputs.
