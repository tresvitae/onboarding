# Release Process

1. Ensure `npm run check` passes locally.
2. Confirm `CHANGELOG.md` contains release-ready notes.
3. Run release-readiness prompt from `.github/prompts/release-readiness.prompt.md`.
4. Create release commit and tag using semantic versioning.
5. Publish release notes from changelog entries.
